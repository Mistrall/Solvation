<UserControl x:Class="Solvation.UI.UIComponents.Frames.SchedulingProblemFrame"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:converters="clr-namespace:Solvation.UI.UIComponents.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<UserControl.Resources>
		<converters:RowToPositionConverter x:Key="EquipmentRowAsYCoordinate"
                                  RowHeight="24"
                                  RowSpacing="0"/>
	</UserControl.Resources>
	<DockPanel x:Name="LayoutRoot">
		<ItemsControl Width="100" DockPanel.Dock="Left" ItemsSource="{Binding Path=Jobs}">
			<ItemsControl.ItemTemplate>
				<DataTemplate>
						<TextBlock Text="{Binding Name}"></TextBlock>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
			<ItemsControl.ItemsPanel>
				<ItemsPanelTemplate>
					<Canvas />
				</ItemsPanelTemplate>
			</ItemsControl.ItemsPanel>
			<ItemsControl.ItemContainerStyle>
				<Style>
					<Setter Property="Canvas.Top" Value="{Binding Number, Converter={StaticResource EquipmentRowAsYCoordinate}}" />
				</Style>
			</ItemsControl.ItemContainerStyle>
		</ItemsControl>
		
	</DockPanel>
</UserControl>
